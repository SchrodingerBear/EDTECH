# Innovatech PH — environment configuration
# Copy to .env and fill real values. .env is git-ignored and web-blocked.
# Every value here is read by includes/config.php at boot.

# Public base URL override (leave empty to auto-derive from the request).
APP_URL=https://hydroguard.site/TITLE%201

# Database — Hosting credentials
DB_HOST=localhost
DB_PORT=3306
DB_NAME=u467106394_thesis
DB_USER=u467106394_thesis
DB_PASS=#j>7Rl2W:8m
