# Lavadora — Laundry Management System (Thesis 2)
# Copy this file to .env and fill real values. .env is git-ignored and web-blocked.
# Every value here is read by includes/config.php at boot.
#
# ONLINE HOSTING (Hostinger):
#   Use the SAME DB_USER / DB_PASS as TITLE 1 (same Hostinger MySQL user).
#   Only DB_NAME differs per thesis. Create a 2nd database on the same account:
#     TITLE 1 -> DB_NAME=u467106394_thesis1
#     TITLE 2 -> DB_NAME=u467106394_thesis2
#   Set DB_HOST to the Hostinger MySQL host (often localhost) and your real DB_PASS.

# Public base URL override (leave empty to auto-derive from the request).
# On hosting, set to your deployed URL, e.g. APP_URL=https://yoursite.com/TITLE2
APP_URL=

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=lavadora_laundry
DB_USER=root
DB_PASS=
